name: easymed
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0
  firebase_storage: ^13.0.4
  firebase_ui_auth: ^3.0.1
  go_router: ^17.0.0
  image_picker: ^1.2.1
  flutter_riverpod: ^2.6.1
  google_sign_in: ^6.2.1
  http: ^1.2.0
  file_picker: ^8.0.0+1
  url_launcher: ^6.3.1
  intl: ^0.20.2
  stream_video: ^1.0.0
  stream_video_flutter: ^1.0.0
  flutter_dotenv: ^5.1.0
  cloud_functions: ^6.0.4
  permission_handler: ^12.0.1
  firebase_messaging: ^16.0.4
  rxdart: ^0.28.0
  flutter_local_notifications: ^19.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  # .env is optional - not included in assets to avoid CI/CD build failures
  # Environment variables should be set directly in CI/CD or create .env locally
  # assets:
  #   - .env
