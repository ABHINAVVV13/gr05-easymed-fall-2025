name: easymed
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0
  firebase_storage: ^13.0.4
  firebase_app_check: ^0.4.1+2
  firebase_ui_auth: ^3.0.1
  go_router: ^17.0.0
  image_picker: ^1.2.1
  flutter_riverpod: ^2.6.1
  google_sign_in: ^6.2.1
  http: ^1.2.0
  file_picker: ^8.0.0+1
  url_launcher: ^6.3.1
  intl: ^0.20.2
  stream_video: ^1.0.0
  stream_video_flutter: ^1.0.0
  flutter_dotenv: ^5.1.0
  cloud_functions: ^6.0.4
  permission_handler: ^12.0.1
  pdf: ^3.11.0
  printing: ^5.10.0
  share_plus: ^12.0.1
  path_provider: ^2.1.2
  flutter_stripe: ^11.1.0
  just_audio: ^0.9.40
  audioplayers: ^6.0.0
  flutter_sound: ^9.2.13
  syncfusion_flutter_charts: ^31.2.16
  firebase_messaging: ^15.1.3


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - .env
  # .env file is NOT included in assets to avoid CI/CD build failures
  # Local dev: Create .env file manually in project root
  # CI/CD: Use GitHub Actions secrets (environment variables)
  # The code handles both cases automatically
